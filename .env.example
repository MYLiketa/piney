# 暂不使用环境变量

# Piney 环境变量配置
# 这些环境变量主要用于 Docker 部署

# 运行模式: server (Docker/服务器) 或 app (Tauri 桌面应用)
# RUN_MODE=server

# 服务端口 (默认: 9696)
# PORT=9696

# 数据目录 (默认: ./data)
# Docker 部署时会自动设置为 /app/data
# DATA_DIR=/app/data

# 配置文件路径 (默认: data/config.yml)
# CONFIG_FILE=data/config.yml
